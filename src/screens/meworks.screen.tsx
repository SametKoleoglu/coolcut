import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const MeWorks = () => {
  return (
    <SafeAreaView>
      <Text>MeWorks</Text>
    </SafeAreaView>
  );
};

export default MeWorks;

const styles = StyleSheet.create({});
